{
  "framework": null,
  "buildCommand": "chmod +x node_modules/.bin/hexo && npm run build",
  "outputDirectory": "public",
  "installCommand": "npm install",
  "env": {
    "NODE_VERSION": "18",
    "NPM_FLAGS": "--legacy-peer-deps --unsafe-perm=true"
  }
}